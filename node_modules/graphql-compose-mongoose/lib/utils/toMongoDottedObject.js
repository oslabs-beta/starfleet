"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.toMongoDottedObject = toMongoDottedObject;
exports.toMongoFilterDottedObject = toMongoFilterDottedObject;

var _mongoose = require("mongoose");

var _graphqlCompose = require("graphql-compose");

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(source, true).forEach(function (key) { _defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(source).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

const primitives = [_mongoose.Types.ObjectId, Date, String, Number, Boolean, _mongoose.Types.Decimal128];

function isPrimitive(value) {
  return primitives.some(p => value instanceof p);
}

function _toMongoDottedObject(obj, target = {}, path = [], filter = false) {
  if (!(0, _graphqlCompose.isObject)(obj) && !Array.isArray(obj)) return obj;
  if (isPrimitive(obj)) return obj;
  const objKeys = Object.keys(obj);
  /* eslint-disable */

  objKeys.forEach(key => {
    if (key.startsWith('$')) {
      const val = Array.isArray(obj[key]) ? obj[key].map(v => _toMongoDottedObject(v, {}, [], filter)) : _toMongoDottedObject(obj[key], {}, [], filter);

      if (path.length === 0) {
        target[key] = val;
      } else {
        target[path.join('.')] = _objectSpread({}, target[path.join('.')], {
          [key]: val
        });
      }
    } else if (Object(obj[key]) === obj[key] && !isPrimitive(obj[key])) {
      _toMongoDottedObject(obj[key], target, Array.isArray(obj) && filter ? path : path.concat(key), filter);
    } else {
      target[path.concat(key).join('.')] = obj[key];
    }
  });

  if (objKeys.length === 0) {
    target[path.join('.')] = obj;
  }

  return target;
  /* eslint-enable */
}
/**
 * Convert object to dotted-key/value pair
 * { a: { b: { c: 1 }}} ->  { 'a.b.c': 1 }
 * { a: { $in: [ 1, 2, 3] }} ->  { 'a': { $in: [ 1, 2, 3] } }
 * { a: { b: { $in: [ 1, 2, 3] }}} ->  { 'a.b': { $in: [ 1, 2, 3] } }
 * { a: [ { b: 1 }, { c: 2 }]} -> { 'a.0.b': 1, 'a.1.c': 2 }
 * Usage:
 *   var toMongoDottedObject(obj)
 *   or
 *   var target = {}; toMongoDottedObject(obj, target)
 *
 * @param {Object} obj source object
 * @param {Object} target target object
 * @param {Array} path path array (internal)
 */


function toMongoDottedObject(obj, target = {}, path = []) {
  return _toMongoDottedObject(obj, target, path);
}
/**
 * Convert object to dotted-key/value pair
 * { a: { b: { c: 1 }}} ->  { 'a.b.c': 1 }
 * { a: { $in: [ 1, 2, 3] }} ->  { 'a': { $in: [ 1, 2, 3] } }
 * { a: { b: { $in: [ 1, 2, 3] }}} ->  { 'a.b': { $in: [ 1, 2, 3] } }
 * { a: [ { b: 1 }, { c: 2 }]} -> { 'a.b': 1, 'a.c': 2 }
 * Usage:
 *   var toMongoFilterDottedObject(obj)
 *   or
 *   var target = {}; toMongoFilterDottedObject(obj, target)
 *
 * @param {Object} obj source object
 * @param {Object} target target object
 * @param {Array} path path array (internal)
 */


function toMongoFilterDottedObject(obj, target = {}, path = []) {
  return _toMongoDottedObject(obj, target, path, true);
}